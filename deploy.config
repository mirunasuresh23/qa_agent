# Deployment Configuration
# Single source of truth for all deployment parameters
# Used by both deploy-all.sh and cloudbuild.yaml

# Google Cloud Project Configuration
PROJECT_ID=leyin-sandpit
REGION=us-central1

# Service Names
FRONTEND_SERVICE=data-qa-agent-frontend2
BACKEND_SERVICE=data-qa-agent-backend2

# Container Registry
REGISTRY=gcr.io

# Optional: Cloud Build Configuration
# TRIGGER_BRANCH=feature_leyin2
# BUILD_TIMEOUT=1200s
